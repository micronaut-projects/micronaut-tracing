plugins {
    id 'io.micronaut.build.internal.parent'
    id 'io.micronaut.build.internal.dependency-updates'
}

repositories {
    mavenCentral()
}

// Workaround for duplicated doc file
tasks.named("processConfigProps") {
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
}
